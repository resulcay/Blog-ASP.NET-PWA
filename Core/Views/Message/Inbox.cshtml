@using EntityLayer.Concrete
@model List<Message>

@{
    ViewData["Title"] = "Inbox";
    Layout = "~/Views/Shared/WriterLayout.cshtml";
}
<style>
    .endless {
        display: flex;
        justify-content: space-between;
    }
</style>
<div class="endless">
    <h1>Gelen Kutusu</h1>
    <button onclick="window.location.href='/Message/SendBox/'" style="text-align:center" class="btn btn-outline-danger btn-fw">Giden Kutusu</button>
</div>
<div class="table-responsive">
    <table class="table">
        <thead>
            <tr>
                <th><h5>ID</h5></th>
                <th><h5>Konu</h5></th>
                <th><h5>Mesaj İçeriği</h5></th>
                <th><h5>Atılma Tarihi</h5></th>
                <th><h5>Gönderen Adı</h5></th>
                <th><h5>Gönderen Maili</h5></th>
                <th><h5></h5></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td><h4>@item.MessageID</h4></td>
                    <td>@item.Subject</td>
                    <td>@item.MessageDetails</td>
                    <td>@item.MessageDate</td>
                    <td>@{ if (item.SenderUser.WriterID == item.ReceiverUser.WriterID)
                            {
                                <span>@item.SenderUser.WriterName (Ben)</span>
                            }
                            else
                            {
                                <span>@item.SenderUser.WriterName</span>
                            } }
                    </td>
                    <td>@item.SenderUser.WriterMail</td>
                    <td>
                        <button class="btn btn-danger" style="width:60px; padding: 8px 10px; font-size: 14px; text-align: center;"
                                onclick="confirmDelete('@item.MessageID')">
                            Sil
                        </button>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<br />
<div class="d-flex justify-content-center">
    <a href="/Message/SendMessage/" class="btn btn-inverse-primary">Yeni Mesaj Gönder</a>
</div>

<script>
    function confirmDelete(messageId) {
        Swal.fire({
            title: 'Sil?',
            text: 'Mesajı silmek istediğinize emin misiniz?',
            showDenyButton: true,
            confirmButtonText: 'Evet',
            denyButtonText: 'Hayır',
            customClass: {
                actions: 'my-actions',
                cancelButton: 'order-1 right-gap',
                confirmButton: 'order-2',
                denyButton: 'order-3',
            },
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = '/Message/DeleteMessage/' + messageId;
            }
        })
    }
</script>
<style>
    .table thead th {
        border-bottom: 1px solid #dee2e6;
    }

    .table tbody tr:first-child {
        border-top: none;
    }

    .table tbody tr:last-child {
        border-bottom: 1px solid #dee2e6;
    }
</style>

